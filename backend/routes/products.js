const express = require("express");
const router = express.Router();

// Product Routes
router.get("/products", async (req, res) => {
  // Implement this endpoint that returns a list of products in json format
});

module.exports = router;
